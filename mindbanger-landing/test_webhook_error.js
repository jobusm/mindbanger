require('dotenv').config({ path: '.env.local' }); const Stripe = require('stripe'); const stripe = Stripe(process.env.STRIPE_SECRET_KEY); const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); stripe.subscriptions.retrieve('sub_1TBh96KdLMFZXQppWPBRjRoz').then(async subscription => { try { const res = await supabase.from('subscriptions').upsert({ id: subscription.id, user_id: 'f3253f4d-6cd6-4a8c-b624-98cf6a2dd7c8', status: subscription.status, price_id: subscription.items.data[0].price.id, current_period_end: new Date(subscription.current_period_end * 1000).toISOString(), country: 'SK', amount_total: 799/100, currency: 'eur', customer_email: 'jobusmiro@gmail.com' }); console.log(res.error || 'success'); } catch (e) { console.error('Supabase error:', e); } }).catch(console.error);
